
<% if user_signed_in? %>

	<div class="panel panel-default">
		<div class="panel-heading">
			<div class="panel-title">
				<h1>What have your friends been up to?</h1>
			</div>
		</div>
		<div class="panel-body">
			It little profits that an idle king, by this still hearth, among these
			barren crags, matched with an aged wife I mete and dole unequal laws unto a savage race, that hoard and sleep and feed and know not me.
		</div>
	</div>

	<div class="row">
		<div class="col-md-8">
			<%= render 'shared/home' %>
		</div>

		<div class="col-md-4">
			<div class="panel panel-default">
				<div class="panel-heading">
					<div class="panel-title">
						<h1><%= current_user.name %></h1>
					</div>
				</div>
				<div class="panel-body">
					<% if current_user.avatar? %>

						<%= image_tag current_user.avatar.url(:medium)%>
						
					<% else %>
						<p>Sorry, no image found</p>
					<% end %>
				</div>

				<div class="panel-footer">
					<%= render 'shared/stats' %>
				</div>
			</div>
		</div>
	</div>

<% else %>


	<div class="jumbotron center semitrans">
		<h1>Welcome to Hikeabl!</h1>
		<h3>Where we know what's up</h3>

		<p>
		    <%= link_to "Log in", new_user_session_path, class: "btn btn-default btn-lg" %>
		    <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
		</p>
		  	
	</div>

<% end %>



